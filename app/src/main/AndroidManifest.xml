<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.GS">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".java.controller.update"></activity>
        <activity android:name=".java.controller.authentificate" />
        <activity android:name=".java.controller.add_sal" />
        <activity android:name=".java.controller.liste_Salaries" />
        <activity android:name=".java.controller.accueil" />
        <activity android:name=".java.controller.infos_salarie" />
        <activity android:name=".java.controller.Registration">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>